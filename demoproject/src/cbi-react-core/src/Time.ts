export default class Time {
    static now(){
        return this
    }

    static addMinutes(minute: number){}

    static addSeconds(seconds: number){}

    static addHours(hours: number){}

    static addMonth(month: number){}

    static addDay(day: number){}


}
